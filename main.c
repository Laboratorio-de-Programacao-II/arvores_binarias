#include <stdio.h>
#include <string.h>
#include "arvore.h"

int main() {
    TipoDicionario Dicionario;
    TipoRegistro x;

    Inicializa(&Dicionario);

    /* =====================================================
     * IMPLEMENTAR: ler comandos da entrada ate o fim do
     * arquivo e executar as operacoes correspondentes.
     *
     * Comandos:
     *   i X   -> Insere o registro com chave X na arvore
     *   r X   -> Retira o registro com chave X da arvore
     *   pre   -> Imprime a arvore em pre-ordem
     *   pos   -> Imprime a arvore em pos-ordem
     *
     * ===================================================== */

    return 0;
}
